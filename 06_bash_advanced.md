# 📜 Day 6: Advanced Bash Scripting (Control Flow & Automation)

## Key Concepts Learned:
- **Conditionals (`if/else`)**: Executing logic based on system thresholds (e.g., Disk usage check).
- **Loops (`for`)**: Iterating over lists of services/files to automate checks.
- **Command Output Parsing**: Extracting specific data using `awk` and `sed`.

## Commands & Scripts Practiced:
- Created `06_resource_monitor.sh` to monitor disk capacity and check service statuses automatically.
- Practiced string trimming and comparison logic inside Bash scripts.
